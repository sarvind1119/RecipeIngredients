# Recipe document importer only. Deliberately NOT in requirements.txt: the Mess
# host that only serves the app does not need PDF/OCR stacks. Install this on
# the machine that converts the department's documents (and hosts import):
#
#     pip install -r requirements-import.txt

# Digital PDFs. Word (.docx) is read with the standard library; spreadsheets use
# openpyxl, which requirements.txt already installs for the Excel export.
pdfplumber

# Groq vision/text for photos, scans, handwriting, and flagged lines.
# Requires GROQ_API_KEY in recipe_import/.env (or the process environment).
groq
python-dotenv
pillow

# Local OCR (ONNX, pip-only - no PaddlePaddle, no Tesseract install). Reads every
# photo alongside Groq and cross-checks its figures; reads alone when Groq is
# unavailable. See recipe_import/ocr_verify.py.
rapidocr
onnxruntime

# Older offline OCR for scans when Groq is unavailable. Optional and heavy — skip
# both lines if you always use Groq vision for images.
# paddleocr
# paddlepaddle
